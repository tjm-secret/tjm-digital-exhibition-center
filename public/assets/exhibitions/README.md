# 展覽資源配置指南

本目錄包含線上展覽網站的範例展覽內容和配置檔案，展示三種不同的資源載入模式。

## 📁 目錄結構

```
exhibitions/
├── sample/                          # 基本範例展覽
├── static-mode-example/             # 靜態模式範例
├── api-mode-example/                # API 模式範例
├── hybrid-mode-example/             # 混合模式範例
└── README.md                        # 本說明文件
```

## 🔧 三種資源載入模式

### 1. 靜態檔案模式 (Static Mode)

**範例**: `static-mode-example/`

**適用場景**:
- 小型展覽 (< 50 個場景)
- CDN 部署
- 離線展示
- 內容不常更新

**優點**:
- 部署簡單
- 載入速度快
- 無需後端支援
- 支援離線瀏覽

**缺點**:
- 內容更新需重新部署
- 不適合大型展覽
- 無法動態管理內容

**配置範例**:
```json
{
  "resourceConfig": {
    "mode": "static",
    "staticPath": "/assets/exhibitions/",
    "fallbackStrategy": "none"
  }
}
```

**檔案結構**:
```
static-mode-example/
├── config.json                     # 展覽配置檔案
├── images/                          # 圖片資源
│   ├── scene-1.jpg                 # 主圖片
│   ├── scene-1-thumb.jpg           # 縮圖
│   └── ...
└── audio/                           # 音訊資源
    ├── scene-1-zh.mp3              # 中文語音
    ├── scene-1-en.mp3              # 英文語音
    └── ...
```

### 2. API 模式 (API Mode)

**範例**: `api-mode-example/`

**適用場景**:
- 大型展覽 (> 100 個場景)
- 需要即時內容管理
- 多展覽平台
- 內容經常更新

**優點**:
- 內容可即時更新
- 支援動態配置
- 集中化管理
- 支援使用者權限控制

**缺點**:
- 需要後端 API 支援
- 部署較複雜
- 依賴網路連線

**配置範例**:
```json
{
  "resourceConfig": {
    "mode": "api",
    "apiEndpoint": "https://api.example.com/exhibitions",
    "fallbackStrategy": "static"
  }
}
```

**API 端點需求**:
- `GET /api/exhibitions/{id}`: 取得展覽配置
- `GET /api/media/images/{filename}`: 取得圖片
- `GET /api/media/audio/{filename}`: 取得音訊

### 3. 混合模式 (Hybrid Mode)

**範例**: `hybrid-mode-example/`

**適用場景**:
- 中大型展覽
- 需要 CDN 加速
- 配置動態管理 + 媒體快取
- 最佳效能需求

**優點**:
- 結合 API 靈活性和 CDN 效能
- 配置可即時更新
- 媒體檔案快速載入
- 最佳的使用者體驗

**缺點**:
- 配置最複雜
- 需要 API 和 CDN 支援
- 成本較高

**配置範例**:
```json
{
  "resourceConfig": {
    "mode": "hybrid",
    "apiEndpoint": "https://api.example.com/exhibitions",
    "cdnBaseUrl": "https://cdn.example.com/exhibitions/",
    "fallbackStrategy": "static"
  }
}
```

## 📋 JSON 配置檔案格式詳解

### 基本結構

```json
{
  "id": "exhibition-unique-id",        // 展覽唯一識別碼
  "title": "展覽標題",                 // 展覽名稱
  "description": "展覽描述",           // 展覽簡介
  "scenes": [...],                     // 場景陣列 (詳見下方)
  "defaultLanguage": "zh",             // 預設語言
  "availableLanguages": ["zh", "en"],  // 支援語言列表
  "resourceConfig": {...},             // 資源載入配置
  "settings": {...}                    // 展覽設定
}
```

### 場景 (Scene) 配置

```json
{
  "id": "scene-1",                     // 場景唯一識別碼
  "title": "場景標題",                 // 場景名稱
  "description": {                     // 多語言描述
    "zh": "中文描述",
    "en": "English description",
    "ja": "日本語の説明"
  },
  "image": {                           // 圖片配置
    "url": "scene-1.jpg",             // 圖片檔案名稱或完整 URL
    "alt": "圖片替代文字",             // 無障礙替代文字
    "width": 1920,                     // 圖片寬度 (像素)
    "height": 1080,                    // 圖片高度 (像素)
    "thumbnail": "scene-1-thumb.jpg"   // 縮圖檔案名稱 (可選)
  },
  "audio": {                           // 多語言音訊配置
    "zh": {
      "url": "scene-1-zh.mp3",        // 音訊檔案名稱或完整 URL
      "duration": 30,                  // 音訊長度 (秒)
      "format": "mp3",                 // 音訊格式
      "size": 480000                   // 檔案大小 (bytes)
    },
    "en": {
      "url": "scene-1-en.mp3",
      "duration": 32,
      "format": "mp3",
      "size": 512000
    }
  },
  "order": 1,                          // 場景順序 (數字)
  "metadata": {                        // 作品資訊 (可選)
    "artist": "藝術家姓名",
    "year": "創作年份",
    "medium": "創作媒材",
    "dimensions": "作品尺寸"
  }
}
```

### 資源配置 (ResourceConfig)

```json
{
  "mode": "static|api|hybrid",         // 載入模式
  "staticPath": "/assets/exhibitions/", // 靜態檔案路徑 (static/hybrid 模式)
  "apiEndpoint": "https://api.../",    // API 端點 (api/hybrid 模式)
  "cdnBaseUrl": "https://cdn.../",     // CDN 基礎 URL (hybrid 模式)
  "fallbackStrategy": "static|api|none" // 備用策略
}
```

### 展覽設定 (Settings)

```json
{
  "autoplay": false,                   // 是否自動播放音訊
  "showThumbnails": true,              // 是否顯示縮圖導覽
  "enableKeyboard": true,              // 是否啟用鍵盤控制
  "preloadCount": 2                    // 預載相鄰場景數量
}
```

## 🎨 媒體檔案規格建議

### 圖片檔案

**主要展示圖片**:
- 解析度: 1920x1080 (Full HD) 或更高
- 長寬比: 16:9 (推薦) 或 4:3
- 檔案格式: JPG (照片), PNG (插圖), WebP (現代瀏覽器)
- 檔案大小: < 2MB (建議 < 1MB)
- 品質設定: JPG 85-90%

**縮圖圖片**:
- 解析度: 300x200 或 400x300
- 檔案格式: JPG 或 WebP
- 檔案大小: < 100KB
- 品質設定: JPG 70-80%

### 音訊檔案

**語音導覽音訊**:
- 檔案格式: MP3 (主要), OGG (備用)
- 位元率: 128kbps (語音) 或 192kbps (音樂)
- 取樣率: 44.1kHz
- 聲道: 單聲道 (語音) 或立體聲 (音樂)
- 檔案大小: < 5MB
- 長度: 30-120 秒 (建議 60 秒內)

## 🌐 多語言支援

### 支援的語言代碼

- `zh`: 中文 (繁體或簡體)
- `en`: 英文
- `ja`: 日文
- `ko`: 韓文
- `fr`: 法文
- `de`: 德文
- `es`: 西班牙文

### 多語言內容組織

1. **描述文字**: 在 `description` 物件中為每種語言提供對應的文字
2. **音訊檔案**: 在 `audio` 物件中為每種語言提供對應的音訊檔案
3. **檔案命名**: 使用語言代碼作為後綴，如 `scene-1-zh.mp3`

## 🚀 快速開始

### 1. 建立新展覽

1. 在 `exhibitions/` 目錄下建立新資料夾
2. 複製 `sample/config.json` 作為範本
3. 修改展覽資訊和場景內容
4. 放置對應的圖片和音訊檔案

### 2. 選擇載入模式

根據您的需求選擇適合的載入模式：

- **小型展覽**: 使用靜態模式
- **大型展覽**: 使用 API 模式
- **高效能需求**: 使用混合模式

### 3. 測試展覽

1. 啟動開發伺服器: `npm run dev`
2. 在瀏覽器中測試展覽功能
3. 檢查所有媒體檔案是否正確載入

## 🔍 故障排除

### 常見問題

1. **圖片無法載入**: 檢查檔案路徑和檔案名稱是否正確
2. **音訊無法播放**: 確認音訊格式支援和檔案完整性
3. **配置檔案錯誤**: 使用 JSON 驗證工具檢查語法
4. **多語言內容缺失**: 確保所有語言都有對應的內容

### 除錯技巧

1. 使用瀏覽器開發者工具檢查網路請求
2. 查看 Console 中的錯誤訊息
3. 驗證 JSON 配置檔案格式
4. 測試不同載入模式的效能差異

---

更多詳細資訊請參閱專案根目錄的 README.md 檔案。