# 線上展覽網站 (Online Exhibition Website)

基於 Vue 3 + Vite + TypeScript 技術棧的沉浸式線上展覽網站。

## 技術棧

- **前端框架**: Vue 3 with Composition API
- **建置工具**: Vite (快速開發和建置)
- **程式語言**: TypeScript
- **樣式框架**: Tailwind CSS (響應式設計)
- **滑動組件**: Swiper.js (專業的觸控滑動支援)
- **音訊處理**: Howler.js (跨瀏覽器音訊播放)

## 專案結構

```
src/
├── components/
│   └── exhibition/
│       └── ExhibitionApp.vue    # 主要展覽應用組件
├── composables/
│   └── useExhibition.ts         # 展覽狀態管理
├── config/
│   └── exhibition.ts            # 展覽系統配置
├── types/
│   ├── index.ts                 # 核心資料模型
│   ├── components.ts            # 組件介面定義
│   └── resources.ts             # 資源載入介面
└── assets/
    ├── base.css                 # 基礎樣式 (包含 Tailwind CSS)
    └── main.css                 # 主要樣式
```

## 快速開始

### 安裝依賴

```bash
npm install
```

### 開發環境

```bash
npm run dev
```

### 建置生產版本

```bash
npm run build
```

### 執行測試

```bash
npm run test:unit
```

### 型別檢查

```bash
npm run type-check
```

## 功能特色

- ✅ Vue 3 + Vite + TypeScript 專案架構
- ✅ Tailwind CSS 響應式設計
- ✅ 完整的 TypeScript 介面定義
- ✅ 組件化架構設計
- ✅ 狀態管理 (Composables)
- ✅ 資源載入策略配置
- 🚧 Swiper.js 滑動導覽 (待實作)
- 🚧 Howler.js 多語言語音導覽 (待實作)
- 🚧 圖片延遲載入 (待實作)
- 🚧 效能最佳化 (待實作)

## 展覽配置

系統支援三種資源載入模式：

1. **靜態檔案模式** (`static`): 適用於小型展覽或 CDN 部署
2. **API 模式** (`api`): 適用於動態內容管理
3. **混合模式** (`hybrid`): 配置來自 API，媒體檔案來自 CDN

範例配置檔案位於 `public/assets/exhibitions/sample/config.json`

## 開發指南

### 新增展覽場景

1. 在 `public/assets/exhibitions/` 建立展覽資料夾
2. 建立 `config.json` 配置檔案
3. 放置圖片和音訊檔案
4. 更新展覽配置

### 自定義組件

所有組件都使用 TypeScript 介面定義，確保型別安全。主要介面定義在 `src/types/` 目錄中。

## 授權

MIT License